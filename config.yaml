seed: 42

data:
  raw_path: "data/yaleemmlc_admissionprediction_triage.csv"
  target: "esi"

model:
  name: "random_forest"

params:
  n_estimators: 200
  class_weight: "balanced"
  random_state: 42
  n_jobs: -1

paths:
  figures: "figs"
  models: "models"
